#ifndef KPP_AST_H
#define KPP_AST_H

/*
 * KPP currently compiles syntax directly to bytecode. This header is reserved
 * for a future explicit AST layer if tooling, formatting, or static analysis
 * needs one.
 */

#endif
